{
  "tests": [
    {
      "name": "OPTIONS CORS preflight",
      "method": "OPTIONS",
      "path": "/",
      "expectedStatus": 200
    },
    {
      "name": "GET without check_id returns 400",
      "method": "GET",
      "path": "/",
      "expectedStatus": 400,
      "expectedBody": {"error": "string"},
      "bodyMatcher": "partial"
    },
    {
      "name": "GET with non-existent id returns 404",
      "method": "GET",
      "path": "/?check_id=999999999",
      "expectedStatus": 404,
      "expectedBody": {"error": "string"},
      "bodyMatcher": "partial"
    },
    {
      "name": "POST creates pending check and returns 202",
      "method": "POST",
      "path": "/",
      "body": "{}",
      "expectedStatus": 202,
      "expectedBody": {"check_id": "string", "status": "pending"},
      "bodyMatcher": "partial"
    }
  ]
}
